NEXT_PUBLIC_API_BASE_URL=http://localhost:5012/api/v1

# NextAuth (Auth.js) — the client owns the session; the Express API issues tokens.
# Generate a real secret with: openssl rand -base64 33
AUTH_SECRET=replace-with-openssl-rand-base64-33-min-32-chars
AUTH_TRUST_HOST=true
