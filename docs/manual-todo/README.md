# Manual TODO

Non-automatable follow-ups — things that need a human (dashboard clicks, account
creation, real credentials) that code/CLI can't do.

| File | What it covers |
|------|----------------|
| `imagekit-setup.md` | Create an ImageKit account and set the API keys to enable image uploads |

Move a file to `done/` once every item in it is checked off.
