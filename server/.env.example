# Application
NODE_ENV=development
PORT=5012
CLIENT_URL=http://localhost:3100
API_PREFIX=/api/v1

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5434/image_validator_db?schema=public

# Redis
REDIS_URL=redis://localhost:6381
BULLMQ_PREFIX=bull

# Authentication
ACCESS_TOKEN_SECRET=change-me-access-token-secret-min-32-chars-long
ACCESS_TOKEN_EXPIRY=15m
REFRESH_TOKEN_SECRET=change-me-refresh-token-secret-min-32-chars-long
REFRESH_TOKEN_EXPIRY=7d

# Logging
LOG_LEVEL=info

# ImageKit (optional — direct-to-CDN image uploads). Get keys at https://imagekit.io/dashboard.
# Leave blank to disable; POST /api/v1/uploads/image returns 503 until all three are set.
IMAGEKIT_PUBLIC_KEY=
IMAGEKIT_PRIVATE_KEY=
IMAGEKIT_URL_ENDPOINT=
